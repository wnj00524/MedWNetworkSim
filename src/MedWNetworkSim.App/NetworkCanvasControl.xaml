<UserControl x:Class="MedWNetworkSim.App.NetworkCanvasControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <DockPanel>
        <TextBlock FontSize="20"
                   FontWeight="SemiBold"
                   Text="Network Canvas" />
        <TextBlock DockPanel.Dock="Right"
                   Margin="18,0,0,0"
                   VerticalAlignment="Center"
                   FontSize="12"
                   FontWeight="SemiBold"
                   Foreground="{DynamicResource AccentStrongBrush}"
                   Text="{Binding TimelineHeadline}" />
        <TextBlock DockPanel.Dock="Right"
                   FontSize="12"
                   Foreground="{DynamicResource MutedForegroundBrush}"
                   Text="All visible traffic is combined by default; use Layers to focus or highlight." />
    </DockPanel>
</UserControl>
